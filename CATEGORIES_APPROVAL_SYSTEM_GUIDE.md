# 🎯 نظام الموافقات للتصنيفات - دليل شامل

## 📋 نظرة عامة

تم إضافة نظام موافقات احترافي للتصنيفات يسمح بالتحكم في نشر التصنيفات الجديدة قبل ظهورها في الموقع.

### 🌟 الميزات الجديدة

#### 1. **خياران للنشر**
- ✅ **نشر مباشر**: التصنيف يظهر فوراً (approved)
- ⏳ **يحتاج موافقة المدير**: التصنيف ينتظر موافقة المدير (pending)

#### 2. **لوحة الموافقات المعلقة**
- عرض جميع التصنيفات المعلقة
- معلومات تفصيلية عن كل طلب
- أزرار سريعة للموافقة/الرفض
- عداد مباشر للتصنيفات المعلقة

#### 3. **إحصائيات متطورة**
- 📊 إجمالي التصنيفات
- 🟣 التصنيفات الرئيسية
- 🟢 التصنيفات النشطة
- 🟠 التصنيفات المعلقة (مع عداد)

#### 4. **نظام الرفض مع السبب**
- إمكانية رفض التصنيف
- إجبارية كتابة سبب الرفض
- تسجيل تاريخ ووقت الرفض
- معرف المدير الذي رفض

## 🗄️ التغييرات في قاعدة البيانات

### الحقول الجديدة في جدول `categories`:

```sql
-- حالة الموافقة
approval_status TEXT DEFAULT 'approved'
  CHECK (approval_status IN ('pending', 'approved', 'rejected'))

-- هل يحتاج موافقة
requires_approval BOOLEAN DEFAULT false

-- من وافق/رفض
approved_by UUID REFERENCES users(id)

-- تاريخ الموافقة/الرفض
approved_at TIMESTAMPTZ

-- سبب الرفض
rejection_reason TEXT

-- من أنشأ التصنيف
created_by UUID REFERENCES users(id)
```

### حالات التصنيف:
- **`pending`**: معلق - ينتظر موافقة المدير
- **`approved`**: معتمد - ظاهر في الموقع
- **`rejected`**: مرفوض - تم رفضه من المدير

## 🔧 خطوات التطبيق

### الخطوة 1: تطبيق تغييرات قاعدة البيانات

1. افتح **Supabase Dashboard**
   - URL: https://app.supabase.com
   - المشروع: `itptinhxsylzvfcpxwpl`

2. اذهب إلى **SQL Editor**

3. نفذ الملف: `supabase/migrations/add_categories_approval_system.sql`

أو انسخ هذا الكود:

```sql
-- إضافة نظام الموافقات
ALTER TABLE categories 
ADD COLUMN IF NOT EXISTS approval_status TEXT DEFAULT 'approved' CHECK (approval_status IN ('pending', 'approved', 'rejected')),
ADD COLUMN IF NOT EXISTS requires_approval BOOLEAN DEFAULT false,
ADD COLUMN IF NOT EXISTS approved_by UUID REFERENCES users(id),
ADD COLUMN IF NOT EXISTS approved_at TIMESTAMPTZ,
ADD COLUMN IF NOT EXISTS rejection_reason TEXT,
ADD COLUMN IF NOT EXISTS created_by UUID REFERENCES users(id);

-- تحديث التصنيفات الموجودة
UPDATE categories 
SET approval_status = 'approved',
    requires_approval = false
WHERE approval_status IS NULL;
```

### الخطوة 2: تطبيق سياسات RLS

نفذ سياسات RLS من نفس ملف SQL لدعم:
- ✅ المدراء: كل الصلاحيات
- ✅ البائعين: إنشاء تصنيفات معلقة فقط
- ✅ الجميع: قراءة التصنيفات المعتمدة فقط

## 🎨 واجهة المستخدم

### 1. التبويبات (Tabs)

#### تبويب "التصنيفات المعتمدة" (approved)
- عرض جميع التصنيفات المعتمدة
- نظام شجري قابل للطي
- بحث وتصفية

#### تبويب "الموافقات المعلقة" (pending) 🔥
- عرض التصنيفات المعلقة فقط
- معلومات مفصلة عن كل طلب:
  * اسم التصنيف (عربي + إنجليزي)
  * الوصف
  * من أنشأه
  * تاريخ الإنشاء
- أزرار الإجراءات:
  * ✅ موافقة (أخضر)
  * ❌ رفض (أحمر)
  * 👁️ عرض التفاصيل

### 2. النموذج المحسّن

#### قسم جديد: "يحتاج موافقة من المدير"
```
☑️ يحتاج موافقة من المدير
   عند تفعيل هذا الخيار، سيتم إرسال التصنيف للمدير للموافقة
   عليه قبل نشره في الموقع. مفيد للتصنيفات الحساسة مثل
   الأدوية أو المنتجات التي تحتاج مراجعة.
```

### 3. Modal الرفض
- حقل نصي لكتابة سبب الرفض (إلزامي)
- تصميم بارز باللون الأحمر
- أيقونة تحذير
- زر تأكيد يُفعّل فقط عند كتابة السبب

## 📱 سيناريوهات الاستخدام

### سيناريو 1: إنشاء تصنيف عادي (نشر مباشر)
```
1. المدير ينقر "إضافة تصنيف رئيسي"
2. يملأ البيانات
3. يترك خيار "يحتاج موافقة" غير مفعل
4. ينقر "حفظ التغييرات"
➡️ النتيجة: التصنيف يظهر فوراً (approval_status = 'approved')
```

### سيناريو 2: إنشاء تصنيف يحتاج موافقة
```
1. المدير (أو بائع) ينقر "إضافة تصنيف"
2. يملأ البيانات - مثلاً: "أدوية"
3. يفعّل خيار "يحتاج موافقة من المدير" ☑️
4. ينقر "حفظ التغييرات"
➡️ النتيجة: التصنيف يُرسل للموافقة (approval_status = 'pending')
```

### سيناريو 3: المدير يوافق على تصنيف
```
1. المدير يذهب لتبويب "الموافقات المعلقة"
2. يرى التصنيف "أدوية" معلق
3. يراجع التفاصيل
4. ينقر زر "موافقة" ✅
➡️ النتيجة: 
   - التصنيف يصبح معتمداً (approval_status = 'approved')
   - يظهر في الموقع مباشرة
   - يُسجّل معرف المدير ووقت الموافقة
```

### سيناريو 4: المدير يرفض تصنيف
```
1. المدير يذهب لتبويب "الموافقات المعلقة"
2. يرى تصنيف غير مناسب
3. ينقر زر "رفض" ❌
4. يظهر modal لكتابة السبب
5. يكتب: "التصنيف مكرر - يوجد فعلياً"
6. ينقر "تأكيد الرفض"
➡️ النتيجة:
   - التصنيف يُرفض (approval_status = 'rejected')
   - يُحفظ سبب الرفض
   - يُسجّل معرف المدير ووقت الرفض
```

## 🔐 الصلاحيات

### المدراء (admin)
- ✅ إنشاء تصنيفات (مباشر أو معلق)
- ✅ تعديل جميع التصنيفات
- ✅ حذف جميع التصنيفات
- ✅ الموافقة على التصنيفات المعلقة
- ✅ رفض التصنيفات المعلقة
- ✅ عرض جميع التصنيفات (معتمد + معلق + مرفوض)

### البائعين/المطاعم (vendor/restaurant)
- ✅ إنشاء تصنيفات معلقة فقط (requires_approval = true)
- ✅ قراءة التصنيفات المعتمدة
- ✅ قراءة تصنيفاتهم المعلقة الخاصة
- ❌ لا يمكنهم النشر المباشر
- ❌ لا يمكنهم الموافقة/الرفض

### العامة (authenticated/anon)
- ✅ قراءة التصنيفات المعتمدة والنشطة فقط
- ❌ لا يمكنهم رؤية المعلق أو المرفوض

## 📊 الإحصائيات المحدثة

### البطاقات الأربع:
```
┌─────────────────────┐  ┌─────────────────────┐
│ 🟣 إجمالي التصنيفات│  │ 🔵 التصنيفات الرئيسية│
│      15             │  │      6              │
└─────────────────────┘  └─────────────────────┘

┌─────────────────────┐  ┌─────────────────────┐
│ 🟢 التصنيفات النشطة │  │ 🟠 التصنيفات المعلقة │
│      13             │  │      3       🔴3    │  ← مع عداد
└─────────────────────┘  └─────────────────────┘
```

البطاقة البرتقالية قابلة للنقر → تنقل لتبويب المعلقة

## 🎯 حالات الاستخدام المقترحة

### 1. التصنيفات الحساسة
```
✅ أدوية
✅ أسلحة
✅ مواد كيميائية
✅ كحوليات
```

### 2. التصنيفات التي تحتاج مراجعة
```
✅ خدمات مالية
✅ استشارات قانونية
✅ منتجات للبالغين فقط
```

### 3. التصنيفات الجديدة من البائعين
```
✅ أي تصنيف يقترحه بائع
✅ تصنيفات غير موجودة في النظام
```

## 🔄 سير العمل (Workflow)

```mermaid
المستخدم/البائع
    │
    ├─ إنشاء تصنيف
    │
    ├─ اختيار: نشر مباشر أو موافقة مدير
    │
    ├─ حفظ
    │
    ↓
    
إذا "نشر مباشر"        إذا "يحتاج موافقة"
    │                        │
    ↓                        ↓
التصنيف معتمد فوراً    التصنيف معلق (pending)
    │                        │
    │                        ├─ يظهر في قائمة المعلقة
    │                        │
    │                        ├─ المدير يراجع
    │                        │
    │                        ├─ موافقة أو رفض
    │                        │
    │                    ┌───┴───┐
    │                    │       │
    │                 موافقة   رفض
    │                    │       │
    │                    ↓       ↓
    │              معتمد     مرفوض
    │                │       (مع سبب)
    │                │
    └────────────────┴─ يظهر في الموقع
```

## 🧪 الاختبار

### اختبار 1: إنشاء تصنيف مباشر
```
✅ إنشاء تصنيف بدون تفعيل "يحتاج موافقة"
✅ التحقق من ظهوره فوراً في التبويب المعتمد
✅ التحقق من approval_status = 'approved'
```

### اختبار 2: إنشاء تصنيف معلق
```
✅ إنشاء تصنيف مع تفعيل "يحتاج موافقة"
✅ التحقق من ظهوره في تبويب المعلقة
✅ التحقق من approval_status = 'pending'
✅ التحقق من عدم ظهوره في التبويب المعتمد
```

### اختبار 3: الموافقة على تصنيف
```
✅ الذهاب لتبويب المعلقة
✅ النقر على "موافقة"
✅ التحقق من اختفائه من المعلقة
✅ التحقق من ظهوره في المعتمد
✅ التحقق من تسجيل approved_by و approved_at
```

### اختبار 4: رفض تصنيف
```
✅ الذهاب لتبويب المعلقة
✅ النقر على "رفض"
✅ كتابة سبب الرفض
✅ تأكيد الرفض
✅ التحقق من approval_status = 'rejected'
✅ التحقق من حفظ سبب الرفض
```

## 📂 الملفات المحدثة

### ملفات جديدة:
1. `supabase/migrations/add_categories_approval_system.sql` - قاعدة البيانات
2. `CATEGORIES_APPROVAL_SYSTEM_GUIDE.md` - هذا الملف

### ملفات محدثة:
1. `app/admin/categories/page.tsx` - الصفحة الرئيسية
   - إضافة نظام التبويبات
   - إضافة قسم المعلقة
   - إضافة دالات الموافقة/الرفض
   - إضافة modal الرفض
   - إضافة خيار "يحتاج موافقة" في النموذج

### نسخة احتياطية:
- `app/admin/categories/page.tsx.backup` - النسخة القديمة

## 💡 نصائح مهمة

### للمدراء:
- 🔍 راجع التصنيفات المعلقة يومياً
- 📝 كن واضحاً في سبب الرفض
- ⚡ وافق بسرعة على التصنيفات المناسبة
- 🎯 استخدم هذا النظام للتصنيفات الحساسة فقط

### للبائعين:
- ✅ فعّل "يحتاج موافقة" للتصنيفات الجديدة
- 📝 اكتب وصفاً واضحاً للتصنيف
- ⏳ انتظر موافقة المدير قبل البدء بالإضافة

### للمطورين:
- 🔒 RLS يحمي البيانات تلقائياً
- 🎨 UI/UX سلس ومريح
- 📊 كل شيء مسجل (audit trail)
- 🔄 سهل التوسع لإضافة ميزات

## 🚀 التطوير المستقبلي

### أفكار مقترحة:
1. 📧 إشعارات email عند تصنيف جديد معلق
2. 💬 نظام تعليقات بين المدير والبائع
3. 📊 تقارير الموافقات والرفض
4. 🔔 إشعارات في الوقت الفعلي
5. 📱 تطبيق موبايل للموافقات السريعة
6. 🤖 AI للموافقة التلقائية على تصنيفات آمنة
7. 👥 نظام موافقات متعدد المستويات
8. 📝 سجل التغييرات لكل تصنيف

## ⚠️ تنبيهات

### مهم:
- ⚠️ يجب تطبيق SQL أولاً قبل استخدام الميزة
- ⚠️ التصنيفات المرفوضة لا تُحذف، فقط تُخفى
- ⚠️ البائعون لا يمكنهم النشر المباشر بعد الآن (اختياري)

### أمان:
- 🔒 RLS يحمي من الوصول غير المصرح
- 🔐 كل عملية مسجلة (من + متى)
- 👤 لا يمكن تزوير معرف المدير

---

**المطور**: GitHub Copilot (Claude Sonnet 4.5)  
**التاريخ**: 3 يناير 2026  
**الحالة**: ✅ جاهز للاستخدام (بعد تطبيق SQL)

**ملف الإصلاح السريع**: `supabase/migrations/add_categories_approval_system.sql`
