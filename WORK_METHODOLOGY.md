# 📋 نهج العمل الموحد - للنسخ في المحادثات الجديدة

---

## 🎯 النهج الإلزامي للتعديلات والإصلاحات

عند إجراء **أي تعديلات** سواء على الكود أو قاعدة البيانات، يجب اتباع الخطوات التالية **بالترتيب**:

---

## 📝 المرحلة 1: الفحص والتحليل

### 1.1 فحص الكود
- قراءة الملفات ذات العلاقة بالكامل
- فهم البنية الحالية والعلاقات
- تحديد المتغيرات والأنواع المستخدمة
- فحص الأخطاء الموجودة (إن وجدت)

### 1.2 فحص قاعدة البيانات
**إنشاء script فحص أولاً** قبل أي تعديل:
```sql
-- مثال: script لفحص جدول معين
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns
WHERE table_name = 'table_name';

-- فحص Foreign Keys
SELECT * FROM information_schema.table_constraints
WHERE table_name = 'table_name';

-- فحص Indexes، Views، Functions، Triggers
-- ... إلخ
```

**ثم انتظر** حتى أقوم بنسخ النتائج وإرسالها لك.

---

## 🔧 المرحلة 2: التنفيذ

### 2.1 تعديل الكود
بناءً على نتائج الفحص:
- إنشاء/تعديل الملفات المطلوبة
- استخدام الأسماء والأنواع **الفعلية** من قاعدة البيانات
- التأكد من التوافق الكامل مع البنية الموجودة

### 2.2 تعديل قاعدة البيانات
إنشاء SQL Script آمن يحتوي على:
- `IF NOT EXISTS` لتجنب الأخطاء
- استخدام الأسماء **الفعلية** للجداول والأعمدة
- Indexes للأداء
- رسالة نجاح مع إحصائيات

---

## ✅ المرحلة 3: التحقق

- فحص الأخطاء في الكود (`get_errors`)
- اختبار الـ SQL Script
- التأكد من عدم وجود تعارضات

---

## 🚫 ممنوعات

- ❌ **لا تفترض** أسماء الأعمدة أو الجداول
- ❌ **لا تستخدم** `store_id` إذا كان الاسم الفعلي `vendor_id`
- ❌ **لا تنشئ** جداول موجودة مسبقاً
- ❌ **لا تبدأ** التعديل قبل فحص البنية الحالية

---

## ✅ واجبات

- ✅ **دائماً** أنشئ script فحص أولاً
- ✅ **انتظر** نتائج الفحص قبل المتابعة
- ✅ **استخدم** الأسماء الفعلية من قاعدة البيانات
- ✅ **تحقق** من التوافق قبل التنفيذ

---

## 📋 قالب العمل (Template)

### عند طلب تعديل جديد:

```
1️⃣ فهم المطلوب
   ↓
2️⃣ قراءة الكود الحالي (read_file)
   ↓
3️⃣ إنشاء script فحص قاعدة البيانات
   ↓
4️⃣ انتظار نتائج الفحص من المستخدم
   ↓
5️⃣ تحليل النتائج
   ↓
6️⃣ تعديل الكود بناءً على البنية الفعلية
   ↓
7️⃣ إنشاء SQL Script محدث وآمن
   ↓
8️⃣ فحص الأخطاء والتحقق
```

---

## 💡 مثال عملي

### الطلب:
"أضف ميزة المتابعة للمتاجر"

### النهج الصحيح:

#### الخطوة 1 - الفحص:
```
أنا (AI): "سأفحص الكود وقاعدة البيانات أولاً"
```

#### الخطوة 2 - إنشاء script:
```sql
-- inspect-follow-system.sql
SELECT * FROM information_schema.tables 
WHERE table_name LIKE '%follow%';

SELECT * FROM information_schema.columns
WHERE table_name = 'stores';
```

#### الخطوة 3 - الانتظار:
```
أنا (AI): "شغّل هذا الـ script وأرسل النتائج"
```

#### الخطوة 4 - التحليل والتنفيذ:
```
بعد استلام النتائج:
- إذا وجدت جدول "store_followers" يستخدم "vendor_id"
- سأستخدم "vendor_id" في الكود
- سأنشئ SQL script متوافق تماماً
```

---

## 📌 ملاحظات مهمة

1. **المحادثة الطويلة**: عند امتلاء المحادثة، انسخ هذه الوثيقة في المحادثة الجديدة
2. **الاتساق**: استخدم نفس النهج في كل مرة
3. **التوثيق**: احفظ scripts الفحص للرجوع إليها

---

## 🎯 الهدف النهائي

- ✅ كود متوافق 100% مع قاعدة البيانات
- ✅ لا أخطاء في التنفيذ
- ✅ سهولة الصيانة والتطوير
- ✅ وضوح البنية والعلاقات

---

**تم إنشاؤه**: يناير 2026  
**للاستخدام**: في جميع المحادثات القادمة
