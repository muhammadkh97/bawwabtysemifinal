-- ═══════════════════════════════════════════════════════════════
-- 🔍 فحص ما حدث بعد قبول الدعوة
-- ═══════════════════════════════════════════════════════════════

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 1: فحص حالة vendor_staff
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 حالة vendor_staff' as info,
    vs.id,
    vs.vendor_id,
    vs.user_id,
    u.email as "البريد",
    vs.status as "الحالة",
    vs.accepted_at as "تاريخ القبول",
    vs.updated_at as "آخر تحديث"
FROM vendor_staff vs
LEFT JOIN users u ON vs.user_id = u.id
WHERE vs.user_id = '390e50d6-50de-4376-bddc-f394323284d8'
  AND vs.vendor_id = '6186f1a0-7f95-4d54-ac70-391127079a3f'
ORDER BY vs.updated_at DESC;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 2: فحص حالة الدعوة
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 حالة الدعوة' as info,
    si.id,
    si.email as "البريد",
    si.status as "الحالة",
    si.accepted_at as "تاريخ القبول",
    si.expires_at as "تنتهي في",
    CASE 
        WHEN si.expires_at > NOW() THEN 'صالحة'
        ELSE 'منتهية'
    END as "حالة الصلاحية"
FROM staff_invitations si
WHERE si.email = 'muhmdakh@gmail.com'
  AND si.business_id = '6186f1a0-7f95-4d54-ac70-391127079a3f'
ORDER BY si.created_at DESC;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 3: محاكاة الاستعلام الذي تستخدمه الصفحة
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 ما تراه الصفحة' as info,
    si.id,
    si.email,
    si.status,
    si.expires_at
FROM staff_invitations si
WHERE si.email = 'muhmdakh@gmail.com'
  AND si.status = 'pending'
  AND si.expires_at > NOW()
ORDER BY si.created_at DESC;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔔 الجزء 4: فحص الإشعارات المرسلة
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔔 إشعارات صاحب المتجر' as info,
    n.id,
    n.user_id,
    u.email as "البريد",
    n.type as "النوع",
    n.title as "العنوان",
    n.message as "الرسالة",
    n.created_at as "تاريخ الإنشاء"
FROM notifications n
LEFT JOIN users u ON n.user_id = u.id
WHERE n.user_id = '05dc64f1-bc85-4b01-af5e-e15f8688b939'
  AND n.type = 'staff_invitation'
ORDER BY n.created_at DESC
LIMIT 5;
