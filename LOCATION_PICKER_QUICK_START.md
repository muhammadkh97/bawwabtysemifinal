# ✅ تم إضافة خيار تحديد الموقع

## 📍 ما تم إنجازه

تم إضافة خريطة تفاعلية لتحديد موقع المتجر/المطعم في:

### 1. لوحة تحكم البائع
📂 **المسار:** `/dashboard/vendor/my-store`

**الميزات:**
- 🗺️ خريطة Google Maps تفاعلية
- 📍 تحديد الموقع بالنقر على الخريطة
- 🧭 زر "موقعي الحالي" (يستخدم GPS الجهاز)
- ✏️ إدخال يدوي للإحداثيات
- 💾 حفظ تلقائي في قاعدة البيانات

### 2. لوحة تحكم المطعم
📂 **المسار:** `/dashboard/restaurant/settings`

**الميزات:** نفس الميزات أعلاه مع تصميم متناسق مع ألوان لوحة المطعم

## 🚀 خطوات التشغيل

### الخطوة 1: تثبيت المكتبة المطلوبة
شغّل هذا الأمر في PowerShell:
```powershell
.\install-location-picker.ps1
```

أو يدوياً:
```bash
npm install @react-google-maps/api
```

### الخطوة 2: الحصول على Google Maps API Key

1. **اذهب إلى:** https://console.cloud.google.com/
2. **أنشئ مشروع جديد** (أو اختر مشروع موجود)
3. **فعّل APIs:**
   - Maps JavaScript API ✅
   - (اختياري) Geocoding API
   - (اختياري) Places API
4. **أنشئ API Key:**
   - APIs & Services > Credentials
   - اضغط "+ CREATE CREDENTIALS"
   - اختر "API key"
   - انسخ المفتاح

⚠️ **للأمان:** قيّد المفتاح بنطاقك فقط!

### الخطوة 3: أضف المفتاح إلى `.env.local`

افتح الملف `.env.local` وحدّث السطر:
```env
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyD...مفتاحك_الحقيقي_هنا
```

### الخطوة 4: أعد تشغيل السيرفر
```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

## 📱 كيفية الاستخدام

### للبائع:
1. سجل دخول كبائع
2. اذهب إلى: **Dashboard** > **متجري**
3. مرر للأسفل إلى قسم "الموقع الجغرافي"
4. حدد موقعك:
   - **سريع:** اضغط "موقعي الحالي"
   - **دقيق:** اضغط على الخريطة
   - **يدوي:** أدخل خط العرض والطول
5. اضغط "حفظ التغييرات"

### للمطعم:
1. سجل دخول كمطعم
2. اذهب إلى: **Dashboard** > **إعدادات المطعم**
3. مرر للأسفل إلى قسم "الموقع الجغرافي"
4. نفس الخطوات أعلاه

## 🗂️ الملفات المُنشأة/المُعدّلة

### ملفات جديدة:
✅ `components/dashboard/LocationPicker.tsx` - Component الخريطة  
✅ `app/dashboard/restaurant/settings/location-picker.css` - تنسيقات المطعم  
✅ `LOCATION_PICKER_IMPLEMENTATION.md` - التوثيق الكامل  
✅ `install-location-picker.ps1` - سكربت التثبيت  
✅ `LOCATION_PICKER_QUICK_START.md` - هذا الملف

### ملفات معدلة:
✏️ `app/dashboard/vendor/my-store/page.tsx`  
✏️ `app/dashboard/restaurant/settings/page.tsx`

## 💾 البيانات في قاعدة البيانات

يتم حفظ الموقع في جدول `stores` في 4 حقول:
- `lat` - خط العرض (Latitude)
- `lng` - خط الطول (Longitude)
- `latitude` - نسخة احتياطية
- `longitude` - نسخة احتياطية

## 🔧 حل المشاكل

### ❌ الخريطة لا تظهر
**الحل:**
1. تحقق من API Key في `.env.local`
2. تأكد من تفعيل "Maps JavaScript API" في Google Cloud
3. أعد تشغيل السيرفر

### ❌ "موقعي الحالي" لا يعمل
**الحل:**
- اسمح للمتصفح بالوصول للموقع
- في Chrome: اضغط على أيقونة القفل بجانب URL > Site settings > Location > Allow

### ❌ لا يتم حفظ البيانات
**الحل:** شغّل الـ diagnostic script لفحص الجدول:
```sql
SELECT id, store_name, lat, lng, latitude, longitude
FROM stores
LIMIT 5;
```

## 📚 مصادر إضافية

- **التوثيق الكامل:** `LOCATION_PICKER_IMPLEMENTATION.md`
- **تشخيص النظام:** `diagnostic_maps_location_system.sql`
- **Google Maps API Docs:** https://developers.google.com/maps/documentation

## 🎯 الخطوة التالية

بعد التثبيت والاختبار:
1. ✅ سأقوم بإنشاء migration script لإصلاح وتوحيد حقول الموقع
2. ✅ إضافة ميزة حساب المسافة
3. ✅ عرض المتاجر على خريطة في الصفحة الرئيسية
4. ✅ البحث عن أقرب المتاجر/المطاعم

---

**هل تحتاج مساعدة؟** تحقق من قسم "حل المشاكل" أو راجع الـ console logs (F12)
