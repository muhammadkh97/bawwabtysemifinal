'use client';

/**
 * Client-side wrapper for Restaurant Dashboard
 * Provides client-side contexts and features
 */
export default function RestaurantLayoutClient({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
