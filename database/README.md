# تنظيم قاعدة البيانات - بوابتي

## 📁 هيكل المجلدات

### 1️⃣ database/migrations/
ملفات الإضافة والإنشاء للجداول والوظائف الجديدة:
- `add-*.sql` - إضافة حقول أو جداول جديدة
- `create-*.sql` - إنشاء جداول ووظائف
- `APPROVAL_SYSTEM_FIXED.sql` - نظام الموافقات المحدث

### 2️⃣ database/diagnostics/
ملفات التشخيص والفحص:
- `diagnose-*.sql` - تشخيص المشاكل
- `check-*.sql` - فحص البيانات والصلاحيات

### 3️⃣ database/fixes/
ملفات الإصلاحات:
- `fix-*.sql` - إصلاح المشاكل المكتشفة

## 📊 الجداول الرئيسية

### المستخدمون والأدوار
- `users` - معلومات المستخدمين
- `user_roles` - أدوار المستخدمين
- `user_permissions` - صلاحيات محددة

### المنتجات والمتاجر
- `products` - المنتجات
- `categories` - التصنيفات
- `stores` / `vendors` - المتاجر
- `hero_slides` - شرائح Hero الديناميكية

### الطلبات والتوصيل
- `orders` - الطلبات
- `order_items` - تفاصيل الطلبات
- `delivery_assignments` - تخصيصات التوصيل
- `delivery_batches` - دفعات التوصيل

### المدفوعات
- `payout_requests` - طلبات السحب
- `exchange_rates` - أسعار الصرف

### التواصل
- `chats` - المحادثات
- `chat_messages` - الرسائل
- `notifications` - الإشعارات

### التقييمات
- `reviews` - تقييمات المنتجات
- `vendor_reviews` - تقييمات المتاجر

## 🔧 الصيانة الدورية

### تنظيف البيانات القديمة
```sql
-- حذف الإشعارات القديمة (أكثر من 30 يوم)
DELETE FROM notifications 
WHERE created_at < NOW() - INTERVAL '30 days' 
AND is_read = true;

-- حذف السلات المهجورة (أكثر من 7 أيام)
DELETE FROM carts 
WHERE updated_at < NOW() - INTERVAL '7 days';
```

### تحسين الأداء
```sql
-- إعادة بناء الفهارس
REINDEX TABLE orders;
REINDEX TABLE products;
REINDEX TABLE chat_messages;

-- تحديث الإحصائيات
ANALYZE orders;
ANALYZE products;
ANALYZE users;
```

## 🚀 أفضل الممارسات

1. **النسخ الاحتياطي**: قم بعمل نسخة احتياطية قبل أي تعديل كبير
2. **الاختبار**: اختبر الاستعلامات على بيئة التطوير أولاً
3. **التوثيق**: وثق أي تغييرات في الهيكل
4. **الصلاحيات**: تحقق من Row Level Security (RLS) للجداول الجديدة
5. **الفهارس**: أضف فهارس للأعمدة المستخدمة في البحث والفلترة

## 📝 ملاحظات مهمة

- جميع الجداول تستخدم UUID كمفتاح أساسي
- تاريخ الإنشاء والتحديث تلقائي باستخدام `created_at` و `updated_at`
- Row Level Security مفعل على معظم الجداول
- استخدام PostGIS للبيانات الجغرافية
- دعم متعدد العملات في المنتجات والطلبات

## 🔐 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- RLS على جميع الجداول الحساسة
- التحقق من الصلاحيات قبل أي عملية
- تسجيل جميع العمليات الحساسة في Audit Log

---

آخر تحديث: يناير 2026
