-- ═══════════════════════════════════════════════════════════════
-- 🔍 فحص نظام الأدوار للمساعدين
-- ═══════════════════════════════════════════════════════════════

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 فحص دور المستخدم الحالي
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 دور المستخدم' as info,
    u.id,
    u.email,
    u.full_name,
    u.role as "الدور في users",
    u.user_role as "user_role"
FROM users u
WHERE u.id = '390e50d6-50de-4376-bddc-f394323284d8';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 فحص حالته كمساعد
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 حالة المساعد' as info,
    vs.id,
    vs.vendor_id,
    s.store_name as "اسم المتجر",
    vs.status as "الحالة",
    vs.permissions as "الصلاحيات"
FROM vendor_staff vs
LEFT JOIN stores s ON vs.vendor_id = s.id
WHERE vs.user_id = '390e50d6-50de-4376-bddc-f394323284d8'
  AND vs.status = 'active';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔧 فحص دالة get_current_user
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔧 دالة get_current_user' as info,
    p.proname as "اسم الدالة",
    pg_get_functiondef(p.oid) as "تعريف الدالة"
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public' 
  AND p.proname = 'get_current_user';
