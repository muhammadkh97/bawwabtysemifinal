-- ═══════════════════════════════════════════════════════════════
-- 🔍 سكريبت تشخيص نظام الحسابات المساعدة للبائعين
-- ═══════════════════════════════════════════════════════════════

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📋 الجزء 1: بنية جدول vendor_staff
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📋 بنية جدول vendor_staff' as info,
    column_name as "اسم العمود",
    data_type as "نوع البيانات",
    is_nullable as "يقبل NULL",
    column_default as "القيمة الافتراضية",
    character_maximum_length as "الطول الأقصى"
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'vendor_staff'
ORDER BY ordinal_position;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔒 الجزء 2: القيود الفريدة (Unique Constraints)
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔒 القيود الفريدة على vendor_staff' as info,
    tc.constraint_name as "اسم القيد",
    tc.constraint_type as "نوع القيد",
    kcu.column_name as "اسم العمود",
    tc.table_name as "اسم الجدول"
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu 
    ON tc.constraint_name = kcu.constraint_name
WHERE tc.table_schema = 'public'
  AND tc.table_name = 'vendor_staff'
  AND tc.constraint_type IN ('UNIQUE', 'PRIMARY KEY')
ORDER BY tc.constraint_name, kcu.ordinal_position;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔗 الجزء 3: المفاتيح الأجنبية (Foreign Keys)
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔗 المفاتيح الأجنبية' as info,
    tc.constraint_name as "اسم القيد",
    kcu.column_name as "العمود المصدر",
    ccu.table_name as "الجدول المرتبط",
    ccu.column_name as "العمود المرتبط"
FROM information_schema.table_constraints tc
JOIN information_schema.key_column_usage kcu 
    ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage ccu 
    ON ccu.constraint_name = tc.constraint_name
WHERE tc.table_schema = 'public'
  AND tc.table_name = 'vendor_staff'
  AND tc.constraint_type = 'FOREIGN KEY';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 4: فحص البيانات الموجودة في vendor_staff
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 سجلات vendor_staff' as info,
    vs.id,
    vs.vendor_id,
    vs.user_id,
    u.email as "بريد المستخدم",
    u.full_name as "اسم المستخدم",
    vs.permissions as "الصلاحيات",
    vs.status as "الحالة",
    vs.invited_at as "تاريخ الدعوة",
    vs.accepted_at as "تاريخ القبول",
    vs.created_at as "تاريخ الإنشاء"
FROM vendor_staff vs
LEFT JOIN auth.users u ON vs.user_id = u.id
ORDER BY vs.created_at DESC
LIMIT 20;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔍 الجزء 5: فحص التكرارات المحتملة
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔍 فحص التكرارات' as info,
    vendor_id,
    user_id,
    COUNT(*) as "عدد التكرارات",
    ARRAY_AGG(status) as "الحالات",
    ARRAY_AGG(id) as "المعرفات"
FROM vendor_staff
GROUP BY vendor_id, user_id
HAVING COUNT(*) > 1;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📋 الجزء 6: بنية جدول staff_invitations
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📋 بنية جدول staff_invitations' as info,
    column_name as "اسم العمود",
    data_type as "نوع البيانات",
    is_nullable as "يقبل NULL",
    column_default as "القيمة الافتراضية"
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'staff_invitations'
ORDER BY ordinal_position;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 7: فحص الدعوات المعلقة
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 الدعوات المعلقة' as info,
    si.id,
    si.email,
    si.business_type as "نوع العمل",
    si.business_id as "معرف المتجر",
    si.status as "الحالة",
    si.permissions as "الصلاحيات",
    si.invitation_code as "كود الدعوة",
    si.expires_at as "تنتهي في",
    si.created_at as "تاريخ الإنشاء"
FROM staff_invitations si
WHERE business_type = 'vendor'
ORDER BY created_at DESC
LIMIT 10;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔔 الجزء 8: بنية جدول notifications
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔔 بنية جدول notifications' as info,
    column_name as "اسم العمود",
    data_type as "نوع البيانات",
    is_nullable as "يقبل NULL",
    column_default as "القيمة الافتراضية"
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'notifications'
ORDER BY ordinal_position;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔔 الجزء 9: فحص إشعارات staff_invitation
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔔 إشعارات المساعدين' as info,
    n.id,
    n.user_id,
    u.email as "بريد المستخدم",
    n.type as "نوع الإشعار",
    n.title as "العنوان",
    n.message as "الرسالة",
    n.link as "الرابط",
    n.is_read as "مقروء",
    n.created_at as "تاريخ الإنشاء"
FROM notifications n
LEFT JOIN auth.users u ON n.user_id = u.id
WHERE n.type = 'staff_invitation'
ORDER BY n.created_at DESC
LIMIT 10;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🛡️ الجزء 10: فحص سياسات RLS على vendor_staff
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🛡️ سياسات RLS على vendor_staff' as info,
    schemaname as "Schema",
    tablename as "الجدول",
    policyname as "اسم السياسة",
    permissive as "نوع السماح",
    roles as "الأدوار",
    cmd as "الأمر",
    qual as "شرط WHERE",
    with_check as "شرط WITH CHECK"
FROM pg_policies
WHERE schemaname = 'public' 
  AND tablename = 'vendor_staff';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🛡️ الجزء 11: فحص سياسات RLS على notifications
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🛡️ سياسات RLS على notifications' as info,
    schemaname as "Schema",
    tablename as "الجدول",
    policyname as "اسم السياسة",
    permissive as "نوع السماح",
    roles as "الأدوار",
    cmd as "الأمر",
    qual as "شرط WHERE",
    with_check as "شرط WITH CHECK"
FROM pg_policies
WHERE schemaname = 'public' 
  AND tablename = 'notifications';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔧 الجزء 12: فحص دالة create_staff_invitation
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔧 دالة create_staff_invitation' as info,
    p.proname as "اسم الدالة",
    pg_get_function_arguments(p.oid) as "المعاملات",
    pg_get_functiondef(p.oid) as "تعريف الدالة"
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'public' 
  AND p.proname = 'create_staff_invitation';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 13: فحص بيانات البائع الحالي
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 معلومات البائع' as info,
    s.id as "معرف المتجر",
    s.store_name as "اسم المتجر",
    s.user_id as "معرف صاحب المتجر",
    u.email as "البريد الإلكتروني",
    u.full_name as "الاسم الكامل"
FROM stores s
LEFT JOIN auth.users u ON s.user_id = u.id
WHERE s.user_id = '05dc64f1-bc85-4b01-af5e-e15f8688b939';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 14: فحص المساعدين الحاليين لهذا البائع
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 المساعدون الحاليون للبائع' as info,
    vs.id,
    vs.user_id,
    u.email as "البريد الإلكتروني",
    u.full_name as "الاسم",
    vs.status as "الحالة",
    vs.permissions as "الصلاحيات",
    vs.created_at as "تاريخ الإنشاء"
FROM vendor_staff vs
LEFT JOIN auth.users u ON vs.user_id = u.id
WHERE vs.vendor_id IN (
    SELECT id FROM stores WHERE user_id = '05dc64f1-bc85-4b01-af5e-e15f8688b939'
)
ORDER BY vs.created_at DESC;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- ✅ انتهى السكريبت
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
