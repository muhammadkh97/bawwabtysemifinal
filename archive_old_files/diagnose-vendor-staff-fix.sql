-- ═══════════════════════════════════════════════════════════════
-- 🔍 استكمال تشخيص نظام الحسابات المساعدة (إصلاح الأخطاء)
-- ═══════════════════════════════════════════════════════════════

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 1: فحص البيانات الموجودة في vendor_staff (مُصلح)
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 سجلات vendor_staff' as info,
    vs.id,
    vs.vendor_id,
    vs.user_id,
    u.email as "بريد المستخدم",
    vs.permissions as "الصلاحيات",
    vs.status as "الحالة",
    vs.invited_at as "تاريخ الدعوة",
    vs.accepted_at as "تاريخ القبول",
    vs.created_at as "تاريخ الإنشاء"
FROM vendor_staff vs
LEFT JOIN auth.users u ON vs.user_id = u.id
ORDER BY vs.created_at DESC
LIMIT 20;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 2: فحص بيانات البائع الحالي (مُصلح)
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 معلومات البائع' as info,
    s.id as "معرف المتجر",
    s.store_name as "اسم المتجر",
    s.user_id as "معرف صاحب المتجر",
    u.email as "البريد الإلكتروني",
    u.raw_user_meta_data as "بيانات المستخدم"
FROM stores s
LEFT JOIN auth.users u ON s.user_id = u.id
WHERE s.user_id = '05dc64f1-bc85-4b01-af5e-e15f8688b939';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📊 الجزء 3: فحص المساعدين الحاليين لهذا البائع (مُصلح)
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📊 المساعدون الحاليون للبائع' as info,
    vs.id,
    vs.user_id,
    u.email as "البريد الإلكتروني",
    vs.status as "الحالة",
    vs.permissions as "الصلاحيات",
    vs.created_at as "تاريخ الإنشاء"
FROM vendor_staff vs
LEFT JOIN auth.users u ON vs.user_id = u.id
WHERE vs.vendor_id IN (
    SELECT id FROM stores WHERE user_id = '05dc64f1-bc85-4b01-af5e-e15f8688b939'
)
ORDER BY vs.created_at DESC;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔍 الجزء 4: فحص جدول users للبحث عن المستخدم المُراد إضافته
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔍 بحث عن muhmdakh@gmail.com' as info,
    id as "معرف المستخدم",
    email as "البريد الإلكتروني",
    raw_user_meta_data as "البيانات الوصفية",
    created_at as "تاريخ التسجيل"
FROM auth.users
WHERE email = 'muhmdakh@gmail.com';

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔍 الجزء 5: فحص إذا كان هذا المستخدم مضاف بالفعل كمساعد
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔍 فحص المساعد muhmdakh@gmail.com' as info,
    vs.id,
    vs.vendor_id,
    vs.user_id,
    s.store_name as "اسم المتجر",
    vs.status as "الحالة",
    vs.permissions as "الصلاحيات",
    vs.created_at as "تاريخ الإضافة"
FROM vendor_staff vs
LEFT JOIN stores s ON vs.vendor_id = s.id
WHERE vs.user_id IN (
    SELECT id FROM auth.users WHERE email = 'muhmdakh@gmail.com'
);

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔔 الجزء 6: فحص جميع الإشعارات للمستخدم muhmdakh@gmail.com
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔔 إشعارات muhmdakh@gmail.com' as info,
    n.id,
    n.type as "النوع",
    n.title as "العنوان",
    n.message as "الرسالة",
    n.link as "الرابط",
    n.is_read as "مقروء",
    n.created_at as "تاريخ الإنشاء"
FROM notifications n
WHERE n.user_id IN (
    SELECT id FROM auth.users WHERE email = 'muhmdakh@gmail.com'
)
ORDER BY n.created_at DESC
LIMIT 10;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 📋 الجزء 7: فحص بنية جدول users في profiles
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '📋 بنية جدول users (profiles)' as info,
    column_name as "اسم العمود",
    data_type as "نوع البيانات",
    is_nullable as "يقبل NULL"
FROM information_schema.columns
WHERE table_schema = 'public' 
  AND table_name = 'users'
ORDER BY ordinal_position;

-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-- 🔍 الجزء 8: فحص بيانات الحساب المُسجل دخوله حالياً
-- ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SELECT 
    '🔍 بيانات المستخدم الحالي' as info,
    u.id,
    u.email,
    u.raw_user_meta_data,
    u.created_at,
    (SELECT role FROM users WHERE id = u.id) as "الدور من جدول users"
FROM auth.users u
WHERE u.id = '05dc64f1-bc85-4b01-af5e-e15f8688b939';
